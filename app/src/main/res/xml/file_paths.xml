<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Directorio de archivos externos privados -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Directorio de caché externo -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- Directorio de archivos privados internos -->
    <files-path
        name="internal_files"
        path="." />

    <!-- Directorio de caché interno -->
    <cache-path
        name="cache"
        path="." />
</paths>