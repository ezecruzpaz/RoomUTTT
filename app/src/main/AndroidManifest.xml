<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ✅ Permisos para galería y cámara -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- ✅ Permiso para Google Play Services -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- ✅ Permiso para notificaciones (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ✅ Feature para cámara (opcional pero recomendado) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".RoomUtttApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoomUTTT"
        tools:targetApi="31">

        <!-- ✅ FILEPROVIDER PARA LA CÁMARA - IMPORTANTE -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
        ✅✅✅ LOGIN ACTIVITY - PUNTO DE ENTRADA (Verificación invisible) ✅✅✅
        -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.RoomUTTT"
            android:windowSoftInputMode="adjustPan">  <!-- ✅ CAMBIAR A adjustPan -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad Principal (Home) - Para usuarios normales -->
        <activity
            android:name=".ui.home.MainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Dashboard Arrendatario - Para arrendatarios -->
        <activity
            android:name=".ui.renter.RenterDashboardActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Actividad de Registro -->
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:parentActivityName=".ui.auth.LoginActivity"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Actividad de Perfil -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Actividad Creación Cuarto -->
        <activity
            android:name=".ui.room.CreateRoomActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- ✅ Actividad Edición Cuarto -->
        <activity
            android:name=".ui.room.EditRoomActivity"
            android:exported="false"
            android:parentActivityName=".ui.room.AllRoomsActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Términos y condiciones -->
        <activity
            android:name=".ui.terms.TermsActivity"
            android:exported="false"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Registro Arrendatario -->
        <activity
            android:name=".ui.renter.RenterRegistrationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Todos los cuartos -->
        <activity
            android:name=".ui.room.AllRoomsActivity"
            android:exported="false"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Detalles del cuarto -->
        <activity
            android:name=".ui.room.RoomDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Mapa en pantalla completa -->
        <activity
            android:name=".ui.room.FullscreenMapActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- ACTIVIDAD: CHAT -->
        <activity
            android:name=".ui.chat.ChatActivity"
            android:exported="false"
            android:parentActivityName=".ui.room.RoomDetailActivity"
            android:theme="@style/Theme.RoomUTTT"
            android:windowSoftInputMode="adjustResize" />

        <!-- ✅ ACTIVIDAD: CHATS DEL RENTER -->
        <activity
            android:name=".ui.renter.RenterChatsActivity"
            android:exported="false"
            android:parentActivityName=".ui.renter.RenterDashboardActivity"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- ACTIVIDAD: LISTA DE CHATS -->
        <activity
            android:name=".ui.chat.ChatsListActivity"
            android:exported="false"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- ACTIVIDAD: NOTIFICACIONES -->
        <activity
            android:name=".ui.notifications.NotificationsActivity"
            android:exported="false"
            android:parentActivityName=".ui.home.MainActivity"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- SERVICIO: FCM (Notificaciones) -->
        <service
            android:name=".utils.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- API Key de Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDI8U5k2MMq-dC0jCPpEGjy_jk4reFYR3o" />

    </application>

</manifest>