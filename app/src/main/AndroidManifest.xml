<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permisos para galería y cámara -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Feature para cámara (opcional pero recomendado) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />


    <application
        android:name=".RoomUtttApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoomUTTT"
        tools:targetApi="31">

        <!-- Actividad de Login como pantalla principal -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RoomUTTT"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad Principal (Home) -->
        <activity
            android:name=".ui.home.MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Actividad de Registro -->
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:parentActivityName=".ui.auth.LoginActivity"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Actividad de Perfil -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.RoomUTTT" />

        <!-- Actividad  Creacion Cuarto -->
        <activity
            android:name=".ui.room.CreateRoomActivity"
            android:exported="false" />

        <!-- Actividad Terminos y condiciones -->

        <activity
            android:name=".ui.terms.TermsActivity"
            android:exported="false"
            android:theme="@style/Theme.RoomUTTT" />
        <!-- Pantalla cuenta Arrendatario -->

        <activity
            android:name=".ui.renter.RenterRegistrationActivity"
            android:exported="false" />

        <!-- Añade esta línea para AllRoomsActivity -->
        <activity
            android:name=".ui.room.AllRoomsActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".ui.renter.RenterDashboardActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Nueva Actividad de Detalles del Cuarto -->
        <activity
            android:name=".ui.room.RoomDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.RoomUTTT" />



        <!-- API Key de Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDI8U5k2MMq-dC0jCPpEGjy_jk4reFYR3o" />

    </application>

</manifest>